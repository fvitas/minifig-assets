# minifig-assets

Part images for [Minifigs](https://github.com/fvitas/lego-configurator), served over GitHub Pages so
the app's deploy does not pay for them.

`parts/` is generated — it is written by `pnpm parts` in the app repo and pushed here by
`pnpm assets`. Edit nothing by hand.
